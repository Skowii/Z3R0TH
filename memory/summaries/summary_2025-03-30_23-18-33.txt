Create a new dictionary with the same keys as the provided dictionary and values generated by a function using Python's `dict.items()`. Iterate through the dictionary and assign each key-value pair to a new dictionary using a lambda function: `map_values(obj, fn)`. Example: `map_values(users, lambda u : u['age'])` returns `{'fred': 40, 'pebbles': 1}`.