Remove first or last n elements from a JavaScript array using various methods like `Array.prototype.shift()`, `Array.prototype.pop()`, `Array.prototype.splice()`, and `Array.prototype.slice()`. Mutative methods like shift, pop, and splice change the original array, while slice returns a new array without the specified elements. To remove elements based on a condition, use filter and findIndex to locate matching elements before using slice.