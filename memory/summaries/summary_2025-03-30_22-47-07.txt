Find branches containing a specific Git commit using `git branch --contains <commit>`. To find branches not containing a specific commit, use `git branch --no-contains <commit>`. This can be useful for identifying which branches contain a particular bugfix or feature.