Generate a JavaScript array of random integers in a given range using `Math.random()`, combined with simple math operations. Create an empty array with the desired length and use `Math.floor()` to map random numbers to integers within the specified range. Example: `const randomIntArrayInRange = (min, max, n = 1) => Array.from( { length: n }, () => Math.floor(Math.random() * (max - min + 1)) + min ); randomIntArrayInRange(12, 35, 10);`