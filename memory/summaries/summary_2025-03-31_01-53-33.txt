Remove accents from a JavaScript string by using `String.prototype.normalize()` with the "NFD" option to convert it to a normalized Unicode format, then use `String.prototype.replace()` with a regular expression to remove diacritical marks in the range of u0300-u036f. Example: `const removeAccents = str => str.normalize('NFD').replace(/[\u0300-\u036f]/g, ''); removeAccents('Antoine de Saint-Exupéry')`